/// @desc Define viewport.

// Viewport size (margin of 2 tile on each edge)
//###
/*var w = room_width - (4*global.tile_size);
var h = room_height - (4*global.tile_size);

// Define top left corner
var pos = [global.tile_size, global.tile_size];

// Set up camera
view_enabled = true;
view_visible[0] = true;
camera_set_view_size(view_camera[0], w, h);
camera_set_view_pos(view_camera[0], pos[0], pos[1]);
window_set_size(800, 600);
surface_resize(application_surface, w, h);

// Set a delay to update the camera
alarm[0] = 1;*/
